1. 项目背景与痛点
用户痛点：
信息滞后：用户无法第一时间获取跨平台（B站、Twitter、HackerNews等）的突发热点。
信息碎片化：现有的热点榜单只给标题，用户无法看到事件的完整时间线、前因后果和深度分析。
真假难辨：网络信息混杂，缺乏自动化的甄别机制。
解决方案：构建一个全自动的“热点雷达”，利用 AI 对多源信息进行清洗、串联和分析，通过 WebSocket 实时推送到具有科技感的仪表盘上。
2. 核心功能模块 (Features)
2.1 多源聚合爬虫引擎 (Crawler Engine)
数据源：Twitter (X), Bing News, HackerNews, 搜狗微信, Bilibili 热门, Google Trends, Reddit, GitHub Trending (共 8+ 源)。
输入：用户自定义关键词（支持正则和排除词）。
地理围栏：支持按国家/地区（如仅美国、仅中国）过滤热点。
频率：每 1-5 分钟轮询一次各平台 API 或 RSS。
2.2 AI 智能清洗与编排中心 (AI Core)
去重与聚合：将不同平台关于同一事件的消息自动归类。
真假识别：利用 DeepseekAPI 分析消息源的可信度和内容逻辑，标记“高可信”、“待验证”或“谣言”。
时间线生成：针对重大事件（如体育赛事、股市异动），AI 自动生成“事件时间轴”，解决碎片化问题。
摘要生成：将长篇新闻自动压缩为 50 字内的快讯。
2.3 实时通信层 (Socket.IO)
推送类型：
突发警报：当出现高热度新词时，弹窗+声音提醒。
数据流：实时更新排行榜数值（如股票涨跌）。
邮件补发：对于离线期间的重要事件，上线后通过邮件推送摘要。
2.4 前端可视化仪表盘 (React 19 + Aceternity UI)
科技感布局：左侧为“热点瀑布流”，中间为“AI 事件时间轴/地图”，右侧为“实时数据排行榜”。
交互：点击任意热点，中间区域动态加载该事件的详细时间线和 AI 分析。
3. 非功能性需求 (NFR)
性能：首屏加载 < 1s，热点推送延迟 < 3s。
技术栈约束：
后端：Node.js 18+，Express 5 (利用其新的中间件特性)，TypeScript。
数据库：Prisma ORM + SQLite (开发阶段)，支持无缝迁移至 PostgreSQL。
实时库：Socket.IO (解决 WebSocket 断连重连问题，比原生 WebSocket 更稳健)。
爬虫：Axios (并发请求) + Cheerio (DOM 解析)。
AI：DeepseekAPI (需配置 API Key 环境变量)。
前端：React 19 (利用 Actions 等新特性)，Tailwind CSS，TypeScript。